#include<stdio.h>
#include "main.h"
/**
 * main - print  putchar
 *
 * Return: Always 0
 */
void print_alphabet(void);
{
	int i;

	for (i = a ; i <= z ; i++)
	{
		putchar(i);
	}
	putchar('\n\);
	return (0);
}
int main()
{
void print_alphabet(void);
return (0);
}
