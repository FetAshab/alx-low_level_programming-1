#include "main.h"
#include <string.h>
/**
 * _strlen - The length of the string
 * @s: parameter passed 
 * Return: the length of the string
 */
int _strlen(char *s)
{    int str;
     str = strlen(*s);
	return str;
}
